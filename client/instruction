#crie o anbiente virtual para o seu projeto
	python3 -m venv venv
	#uma vez criado, não precisa mais recria-ló para o mesmo projeto

#ative o ambiente
	source venv/bin/activate
	#sempre utilizar quando sair do ambiente

#para facilitar, crie um arquivo  requirements.txt para instalar as
#dependências
	pip3 freeze > requirements.txt
	#uma vez criado, não precisa recria-ló para o mesmo projeto
#no arquivo requirements.txt liste todos os modulos e bibliotecas que
#seu projeto usa
#exemplo
	customtkinter
	packaging

#basta instalar os pacotes listados no requirements.txt
	pip install -r requirements.txt
